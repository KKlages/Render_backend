version: 1.0
runtime: python3.11
build:
  commands:
    pre-build:
      - pip install --upgrade pip
      - pip install -r requirements.txt
run:
  command: gunicorn -b 0.0.0.0:8000 app:app
  network:
    port: 8000
